# Shops / Customers setup

1. Open Supabase Dashboard > SQL Editor.
2. Run `07_SHOPS_SETUP.sql` once.
3. Deploy the updated frontend through GitHub and Netlify.

The module is admin-only and generates shop codes automatically, such as `SHOP-0001`.
